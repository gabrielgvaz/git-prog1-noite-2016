#include <stdio.h>
int main(void) 
{
        int a,b,c;
        
                for(; a-- > 64 ;) {
                        putchar (++c == 'Z' ? c = c/9 : 33^b&1);
                }
        
        return 0;
}