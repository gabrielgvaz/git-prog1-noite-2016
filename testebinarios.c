#include <stdio.h>
int main()
{
    int i=0, j, num, vet[30];
    
    printf("\nDigite o numero decimal: ");
    scanf("\n%d", &num);
    
    while(num>0)
    {
        resto[i] = num%2:
        i++;
        num = num/2;
    }
        printf("\nO número binário é: ");
        for(j=i - 1; j>=0; j--)
        {
            printf("\n%d", vet[j]);
        }
        printf("\n");
        return 0;
}
